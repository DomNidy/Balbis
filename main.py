import pygame
import pygame,sys
import random

#Game where we have to dodge walls

pygame.display.init()
screen_width=700
screen_height=400
screen=pygame.display.set_mode([screen_width,screen_height])
pygame.display.set_caption("Turnaments")

while True:
      if(event.type==QUIT):
            pygame.quit()
            sys.exit
            
